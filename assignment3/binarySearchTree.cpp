#include <iostream>
#include <algorithm>

// node struct construct
struct Node {
            std::string id;
            Node* left;
            Node* right;
};

Node* insert(std::string s) {};
Node* inOrderTraversal() {};